import { Meta } from '@storybook/react'
import { Layout } from './Layout'

export default {
	title: 'Sections/Layout',
	component: Layout
} as Meta

export const Default = () => <Layout>Content</Layout>
